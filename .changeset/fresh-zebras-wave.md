---
"@neuledge/context": patch
---

Improve MCP guidance for concise `get_docs` queries and the registry-first install workflow. Fix scoped package installs (e.g., `@tanstack/react-query`) by sanitizing `/` in filenames.
